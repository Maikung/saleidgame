PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/saleidgame?retryWrites=true&w=majority
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
# Created by connecting a Vercel Blob store to this project. Never expose it to the client.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_replace_me
# Only needed when registering an admin from the test frontend
ADMIN_REGISTRATION_CODE=change-this-admin-code
